# Gold Bot v2 – Python Dependencies
# Install: pip install -r requirements.txt

# Telegram Bot framework (async, v21+)
python-telegram-bot>=21.0
python-telegram-bot[job-queue]>=21.0
python-telegram-bot[webhooks]>=21.0
# Google Sheets client
gspread>=6.0.0

# Google authentication
google-auth>=2.20.0

# HTTP client (image fallback, gold price scraper)
requests>=2.31.0

# HTML parser for tgju.org scraping
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Environment variable loader
python-dotenv>=1.0.0

# Groq AI (default provider — fast inference, OpenAI-compatible API)
groq>=0.9.0
google-genai

# Structured data models for the AI layer (IntentExtraction, CustomerProfile, etc.)
pydantic>=2.6.0

# Async HTTP server required by python-telegram-bot webhook mode
aiohttp>=3.9.0

# ── Optional: uncomment if you implement an additional provider ──────────────
# google-generativeai>=0.5.0   # needed for providers/gemini_provider.py
# openai>=1.30.0               # needed for providers/openai_provider.py
